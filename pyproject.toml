[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Modernise-TradeMaster"
authors = [{name = "H Tan", email = "eewnah1@gmail.com"}]
dynamic = ["version", "description"]

dependencies = [
    "myst-parser>=0.18.1",
]
